# 11 · Backend API — Lorebinge SPEC (RN9)

> Часть разбитого спека. Источник-архив: [`spec-rn9.md`](spec-rn9.md) — не редактировать. Содержит: §8 — Postgres RPC-функции (`SECURITY DEFINER`) и Edge Functions. Оглавление, граф зависимостей и карта перекрёстных ссылок — в [00-index.md](00-index.md).

-----

## 8. Backend API (контракты)

### 8.1 Postgres RPC-функции (`SECURITY DEFINER`)

> Вызываются через `supabase.rpc(...)`. Все возвращают JSON, проверяют `auth.uid()`. Обновлено по ревизии: добавлена `start_lesson` (с возобновлением прогресса урока, §A7), `refill_battery` расширена до `refresh_user_state` (батарея + ленивая переоценка стрика), удалены `buy_streak_freeze`/`apply_streak_freeze` (магазин и ручная заморозка убраны, КРИТ-8); ачивки убраны (ВАЖН-16); **квесты убраны целиком** — `get_quests`/`claim_quest_reward` удалены (глобальное решение); добавлен `get_me` — снимок пользователя одним вызовом для холодного старта (C-6).

|Функция                 |Вход                           |Выход                                                                                                                |Логика (кратко)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
|------------------------|-------------------------------|---------------------------------------------------------------------------------------------------------------------|------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
|`start_lesson`          |`lesson_id uuid`               |`{ ok, resume_task_index }`                                                                                          |**Старт раннера урока (прогресс сохраняется, §A7).** Лениво создаёт `user_course_progress` (`started_at=now()`) и `user_lesson_progress` (`status=active`) при первом входе. **Возобновление:** возвращает `resume_task_index = current_task_index` — раннер открывается с этого задания (не с начала). Если урок уже `completed` (перепрохождение) → `current_task_index` и `errors_count` сбрасываются в 0, `resume_task_index=0` (проходим заново). Если урок `active` и `current_task_index=0` (первый заход) → `errors_count=0`. Если `active` и `current_task_index>0` (продолжение после выхода/окончания заряда) → `errors_count` и индекс **сохраняются**. Вызывается клиентом при открытии экрана урока.|
|`submit_task_attempt`   |`task_id uuid, is_correct bool`|`{ battery, was_correct, is_subscriber }`                                                                            |Записывает attempt и **продвигает `user_lesson_progress.current_task_index`** на следующее задание (по `sort_order`) — прогресс урока сохраняется. Если подписка активна (`trial`/`active`) — батарея не тратится. Иначе для теста (`requires_check=true`): `battery=0` → `BATTERY_EMPTY` (→ paywall; индекс **не** двигается, чтобы при возврате продолжить с этого же задания, §12.9/Gap #3). Иначе `battery -= battery_cost_per_answer` (за любой ответ). `is_correct=false` → `errors_count += 1`. Контентные задания (`requires_check=false`) батарею не трогают, но индекс продвигают.                                                                                                                                                                                                            |
|`complete_lesson`       |`lesson_id uuid`               |`{ unlocked_next_lesson_id, diamonds_awarded, already_rewarded, new_diamond_balance, errors_count, course_completed, leveled_up, new_knowledge_level }`|Проверяет завершённость по курсору: `current_task_index` дошёл до последнего задания (§A7, **не** по «есть attempt у каждой задачи» — та проверка ложно проходила на перепрохождении, C-2). `status=completed`, `errors_count` (число неверных тест-ответов, для маскота), разблокировка следующего урока, обновление `lessons_completed`. **Награда за урок — только алмазы** (заряд за завершение не начисляется, §A4/§12.6). **Первое завершение урока** (`already_rewarded=false`): случайные алмазы из взвешенного `lesson_diamond_rewards` (идемпотентно через unique-индекс `uq_diamond_reward_once` + `insert … on conflict do nothing`; повтор/гонка не доначисляет — §6.7/§A6). **Перепрохождение** уже `completed`-урока (`already_rewarded=true`): фикс `lesson_replay_diamond_reward` (дефолт 1, §A6/§12.6), взвешенный рандом не перекатывается. **Последний урок курса:** курс `completed`, `courses_completed += 1`; каждые `courses_per_level` (дефолт 5) пройденных курсов → `knowledge_level += 1` (`leveled_up=true`, §A4/§12.0). Вызывает `touch_streak`. *(Ачивок и квестов нет — убраны из MVP.)*|
|`touch_streak`          |—                              |`{ current_streak, longest_streak }`                                                                                 |**Внутренняя** (вызывается из `complete_lesson` / `refresh_user_state`, **не** напрямую с клиента). Инкремент стрика по дню; **автозаморозка за алмазы** при пропуске (§A5).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
|`refresh_user_state`    |—                              |`{ battery, next_refill_at, current_streak, longest_streak, streak_broken, frozen_days }`                            |**Заменяет `refill_battery`.** Один серверный вызов при старте/заходе (КРИТ-8): (1) ленивое восстановление батареи по `battery_refill_minutes` (§A4); (2) ленивая переоценка стрика — автозаморозка за алмазы за пропущенные дни (`streak_freeze_cost_per_day`/день, пока хватает баланса; `frozen_days` = сколько списано), иначе `streak_broken=true`, стрик → 0 (§A5); (3) **возврат батареи при истечении подписки (C-1):** если обнаружен переход `trial`/`active` → `expired`/`cancelled`/`none`, выставляет `battery=battery_max`, `last_battery_refill_at=now()` (§A4) — это **единственное** место сброса (переживает отсутствие вебхука; `revenuecat-webhook` только меняет `subscriptions.status`). Так HUD при входе показывает актуальные батарею **и** стрик.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
|`reset_course_progress` |`course_id uuid`               |`{ ok }`                                                                                                             |Удаляет прогресс по курсу.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
|`get_subscription_offer`|—                              |`{ trial_available, trial_days, timer_active, timer_ends_at, timer_expired }`                                        |**Server-authoritative логика предложения (§A14).** Читает `app_config.economy.trial_offer` и `subscriptions`. Ставит `offer_timer_started_at` на первом вызове при включённом таймере. Клиент только отображает.                                                                                                                                                                                                                                                                                                                                                                         |
|`get_home_feed`         |—                              |`{ lores:[…], in_progress:[], popular:[] }`                                                                          |Агрегирует блоки Home одним запросом. Состав полей (НЕКР-12): `lores[]` — карточка Lores с вычисленными `lessons_count`/`tasks_count` (+ поля градиента/декора, §12.3а) **+ `is_completed`** (курс пройден пользователем — `user_course_progress.completed_at != null`; для overlay «пройдено», §12.3а/D); `in_progress[]` — `{ id, title, cover_image_url, lessons_completed, total_lessons }` (**без** `last_lesson_id` — карточка ведёт на `course_path`, §12.3; по определению не завершён); `popular[]` — `{ id, title, cover_image_url, is_completed }`. Все опубликованные курсы. **Категории Home больше не возвращает** (блок Categories убран с Home; их отдаёт `get_catalog` для вкладки Catalog, §12.3г). (Блок Grid — отдельная вкладка → `get_grid_feed`.)                                                                                                                                                                                                                                                                                                                                                             |
|`get_grid_feed`         |—                              |`{ items:[{ course_id, cover_image_url, title, is_completed }] }`                                                    |Лента вкладки **Grid** (§12.3б). `cover_image_url` = обложка **первого урока** курса (`lessons.cover_image_url` с минимальным `sort_order` — это и есть «первое видео»). `is_completed` — курс пройден пользователем (для тёмного overlay + значка «пройдено», §12.3б/D). Все `is_published`-курсы; порядок — `sort_order`/`popularity_score`.                                                                                                                                                                                                                                                                                                                                                  |
|`get_category_courses`  |`category_id uuid`             |`{ category:{ id, name }, courses:[{ id, title, cover_image_url, lessons_count, tasks_count, is_completed }] }`       |Курсы одной категории (§12.3в), только `is_published`. Счётчики — агрегаты, как в `get_home_feed`; `is_completed` — для overlay «пройдено» (§12.3в/D).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
|`get_catalog`           |—                              |`{ categories:[{ id, name, slug, courses_count }] }`                                                                 |**Алфавитный индекс категорий для вкладки Catalog (§12.3г).** Только категории с **≥1 `is_published`-курсом** (пустые не возвращаются); `courses_count` — число опубликованных курсов в категории. Сортировка — по `name` (алфавит, `localeCompare`; `categories.sort_order` не используется). Группировку по буквам делает клиент. Вкладка по умолчанию выключена (`enabled:false`), но RPC работает всегда — данные появятся, как только админ включит Catalog (B6.7).                                                                                                                                                                                                                                                       |
|`get_course_path`       |`course_id uuid`               |`{ course:{ id,title,path_background_color,is_completed }, lessons:[{id,title,cover_image_url,sort_order,status}] }`  |Сетка уроков со статусами (`completed`/`active`/`locked`) для пользователя. Возвращает обложку каждого урока, цвет фона сетки и **`course.is_completed`** (для верхней плашки «Course completed», §12.4/D). **Ленивая инициализация:** при отсутствии строк прогресса статусы вычисляются по правилу «первый по `sort_order` — `active`, остальные `locked`», строки **не** создаются; `user_course_progress`/`user_lesson_progress` создаются позже — в `start_lesson` при первом входе в урок (§A7).                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
|`get_me`                |—                              |`{ profile, streak, subscription, subscription_badge\|null }`                                                        |**Снимок пользователя одним вызовом (bootstrap, C-6/A-3).** Склеивает `profiles` + `streaks` + `subscriptions` + `app_config.subscription_badge` — то, из чего собирается доменный `UserProfile` (§9): `is_subscriber` = `subscription.status ∈ trial/active`, `subscription_badge` отдаётся только подписчику. Read-only (мутации батареи/стрика — в `refresh_user_state`). Снимает множественные round-trip’ы на старте (NFR «≤2–3 c до Home», §15). На холодном старте клиент зовёт `get_me` **+** `refresh_user_state`.|
|`delete_account`        |—                              |`{ ok }`                                                                                                             |**Требование и Google Play, и App Store** (Apple 5.1.1(v)). Реализуется как Edge Function `delete-account` (§8.2) с **сервисной ролью**: вызывает `auth.admin.deleteUser(uid)`, а БД каскадом (`profiles → auth.users ON DELETE CASCADE`, §6) вычищает `public.*`. Прямого `delete from auth.users` из Postgres-RPC не делаем (нужны права Auth Admin) — потому это **Edge**, а не обычный RPC. Клиентский слой по-прежнему вызывает один контракт «удалить аккаунт».                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |

**Транзакционность.** `complete_lesson` — одна транзакция; начисление алмазов за первое прохождение идемпотентно (`insert … on conflict do nothing` по unique-индексу `uq_diamond_reward_once`, §6.7 — гонка/повтор не доначисляют); счётчик курсов/уровня обновляется в той же транзакции. *(Квесты убраны — RPC `get_quests`/`claim_quest_reward` удалены, обновления прогресса квестов в `complete_lesson` больше нет.)*

> **Конкурентность мутаций (инвариант, B-4).** Все RPC, меняющие `profiles.battery`/`profiles.diamonds`/`user_lesson_progress.current_task_index` (`submit_task_attempt`, `complete_lesson`, `refresh_user_state`, `touch_streak`), выполняют изменение **под блокировкой строки пользователя** — `SELECT … FOR UPDATE` перед пересчётом, либо атомарным `UPDATE … SET battery = greatest(0, battery - cost) WHERE … RETURNING`. Это исключает lost-update при параллельных вызовах (быстрые тапы, ретраи TanStack Query): нельзя «ответить дважды за один заряд» или разъехаться по балансу. Без явной блокировки read-modify-write-логика экономики некорректна.

> **`is_completed` курса — единое определение (D-2).** Во всех RPC, возвращающих `is_completed`/`course.is_completed` (`get_home_feed.lores`/`popular`, `get_grid_feed`, `get_category_courses`, `get_course_path`), оно означает **одно и то же**: `exists(user_course_progress WHERE user_id=auth.uid() AND course_id=… AND completed_at is not null)`. Меняем правило «пройденности» — правим здесь, RPC ссылаются на эту сноску (раньше определение дублировалось в каждом RPC).

> **Доверие клиенту в `submit_task_attempt(is_correct)` — осознанный компромисс (НЕКР-15).** Правильные ответы лежат в `payload` (читаемом клиентом), и клиент сам сообщает `is_correct`. Это формально расходится с принципом §3 «не доверяем клиенту», но безопасно: `is_correct` влияет **только** на `errors_count` (вход для тона маскота, F7); экономика (батарея/алмазы) от правильности **не зависит** и считается сервером. Серверную проверку ответов в MVP не строим.

### 8.2 Edge Functions (TypeScript)

|Функция             |Метод         |Вход                          |Выход                             |Назначение                                                                                                                                                                                                                                                                                                                                                          |
|--------------------|--------------|------------------------------|----------------------------------|--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
|`revenuecat-webhook`|POST (webhook)|RevenueCat event payload      |200                               |**Источник истины по подпискам обеих платформ.** Принимает события RevenueCat (initial_purchase, renewal, trial_started, cancellation, expiration, billing_issue) и обновляет `subscriptions` + пишет `payment_events` (с `platform`/`store`, суммой, валютой, страной). На событии `trial_started` дополнительно ставит `subscriptions.trial_used = true` (§A14). Проверка подписи вебхука. **Идемпотентность (B-5):** доставка at-least-once → пишет `payment_events` через `on conflict (event_id) do nothing`; если событие уже обработано (`event_id` есть) — выходит рано, не применяя сайд-эффекты повторно. Заменяет прежний `play-rtdn` и покрывает App Store + Google Play.|
|`validate-purchase` |POST          |`{ rc_app_user_id }` (или чек)|`{ status, expires_at, platform }`|Реконсиляция статуса по запросу клиента после покупки/восстановления: сверяется с RevenueCat REST API, обновляет `subscriptions`. (Fallback без RevenueCat: валидация чека Apple через App Store Server API **или** Google Play Developer API по `platform`.) Ключи — в секретах Edge Functions.|
|`delete-account`    |POST          |— (по сессии)                 |`{ ok }`                          |**Удаление аккаунта (C-3, RPC `delete_account` §8.1 — это его контракт).** Сервисная роль: `auth.admin.deleteUser(auth.uid())`; БД каскадом (`profiles → auth.users ON DELETE CASCADE`, §6) вычищает все `public.*`-данные пользователя. Вынесено в Edge (а не Postgres-RPC), т.к. удаление `auth.users` требует прав Auth Admin. **Требование обоих сторов** (Apple 5.1.1(v)).                                                                    |


> **Поток подписки (RN).** Клиент покупает/восстанавливает через RevenueCat SDK (`react-native-purchases`) → RevenueCat обновляет entitlement → шлёт `revenuecat-webhook` → Edge Function пишет `subscriptions`. Клиент дополнительно может дернуть `validate-purchase` для немедленной реконсиляции. Статус в приложении читается из `subscriptions` (server-authoritative), не из локального кэша SDK.

