# Lorebinge — пайплайн и иерархия промптов (v3)

> Рабочий документ. Весь путь: списки идей/топиков → итеративные апрувы → канон →
> разбиение большого промпта на генераторы B1–B5 → апрув персонажей (2 шага) → апрув
> промптов → производство медиа → поэлементный апрув → импорт. Заменяет прошлую версию.

---

## 0. Две базовые таблицы-списка (основание)

- **Список идей** — короткие зёрна из 2–3 слов. Ещё не курс; из идеи позже может родиться
  топик.
- **Список топиков** — полноценные предложения, которые могут лечь в основу курса. К топику
  можно приложить «кусок сценария» — дополнение к промпту (= `USER_BRIEF`, доходит до B2).

---

## 1–3. Ручной ввод из админки и приоритет очереди

1. **Ручной ввод идеи** (вне апрува): идею одним полем → в очередь как идея (не топик).
2. **Ручной ввод топика** (вне апрува): `топик` + `кусок сценария` → пара в очередь.
3. **Приоритет очереди:** сначала готовые топики; когда кончаются — апрув идей.

---

## A. Механика апрува (ОДНА на все шлюзы ⛔)

Каждый шлюз (идея, топик, канон, фото, промпты, элементы) работает одинаково:

- **✓ Approve** — дальше (где уместно — опц. поле «дописать к промпту»).
- **✗ Reject** — артефакт отбрасывается (для идеи/топика — гибель ветки).
- **✎ Refine** — поле для комментария → **универсальный промпт-переписыватель** берёт
  `{тип артефакта, текущая версия (JSON), твоё замечание, правила этапа}` и возвращает
  **чистую новую версию** (не дописку в конец!), не нарушая жёстких правил этапа → снова
  на апрув. **Цикл повторяется сколько угодно раз, пока не устроит.**

**Почему переписывание, а не дописка:** дописка в конец копит противоречия («Николаю 20» +
снизу «нет, 3») и ломает модель. Переписыватель отдаёт цельный непротиворечивый промпт.
Прозрачность не теряется, потому что:

> **Все версии артефактов + твои замечания хранятся в БД** (история правок) — чистый промпт
> уходит в ИИ, а аудит-трейл остаётся. Это же потом — материал для доработки самого пайплайна.

Переписыватель — ОДИН генерик на все этапы (не серия под каждый), различается только
входным `тип артефакта` + `правила этапа`. Файл: `refine_rewriter.md`.

> **⚠️ ГЛАВНОЕ ПРО МЕДИА-ШЛЮЗЫ (видео §11, картинки/озвучка §12).** На медиа-шлюзе я правлю
> НЕ САМО медиа, а **ПРОМПТ, который его сгенерил**. Механика Refine для медиа:
> 1. refine_rewriter получает на вход `source_prompt` (исходный промпт генерации, НЕ видео-файл)
>    + моё замечание + правила этапа → возвращает **новый чистый промпт**.
> 2. Воркер заново вызывает генератор медиа (omni/Leonardo/ElevenLabs) с этим новым промптом.
> 3. Новое медиа снова летит на тот же шлюз. Цикл §A повторяется.
>
> Поэтому в `review_items` у медиа-элемента ЛЕЖИТ исходный промпт (`source_prompt`, см. §13):
> `content` = ссылка на медиа (что показать в плеере), `source_prompt` = что реально правится.
> `version_history` хранит и старые промпты, и старые ссылки на медиа. Само медиа никогда не
> редактируется — только перегенерируется из переписанного промпта.

---

## 4. Основной процесс (воронка с итеративными апрувами)

```
P1  Генератор идей ──► список идей
                         │ (по одной)
                         ▼
                  ⛔ АПРУВ ИДЕИ            (Approve / Reject)
                         │ ✓
                         ▼
P2  Идея → Топик
                         ▼
                  ⛔ АПРУВ ТОПИКА ⟳        (Reject / Refine+поле / Approve+опц. дописка)
                         │ ✓
                         ▼
P3  Генератор СЦЕНАРИЯ-КУРСА (учитывает дописку/кусок сценария) → КАНОН
     · канон включает РОСТЕР персонажей и их количество
                         ▼
                  ⛔ АПРУВ КАНОНА ⟳        (правлю всё: число уроков, число персонажей,
                         │ ✓                убрать конкретного — любым замечанием; персонажи
                         │                  одобряются ЗДЕСЬ, отдельного апрува списка нет)
                         ▼
            B5 refs → фото персонажей (1 на каждого) ─┐
            B6      → стилевые фото (сколько влезет в omni) ─┤
                         ▼                                   │
                  ⛔ АПРУВ ФОТО (персонажи + стиль) ⟳ ───────┘  (визуальный фундамент,
                         │ ✓                                     ДО видео — дёшево чинить)
                         ▼
            B1/B2/B3/B4 заполняют слоты → ПРОМПТЫ
                         ▼
                  ⛔ АПРУВ ПРОМПТОВ ⟳      (каждый промпт + RU-перевод + RU-пересказ; §8.5)
                         │ ✓
                         ▼
            Производство медиа (omni/Leonardo/ElevenLabs)  (§9)
              ├─ видео 1 готово → ⛔ АПРУВ ВИДЕО 1 ⟳  (поштучно, §11)
              ├─ видео 2 готово → ⛔ АПРУВ ВИДЕО 2 ⟳
              └─ видео 3 готово → ⛔ АПРУВ ВИДЕО 3 ⟳
                         ▼
                  ⛔ АПРУВ ОСТАЛЬНОГО ⟳    (батч: все картинки курса + все тексты; §12)
                         │ ✓
                         ▼
                  Импорт в public → приложение + маркетинг
                         ▲
                     P4  Доработка одного элемента (по Refine-комментарию)
```

`⟳` = итеративная петля апрува (см. §A): правлю → возвращается → пока не устроит.

---

## 4.1. Пропущенные шлюзы (skipped) — бизнес-правило

Не каждый проект проходит все шлюзы воронки; часть гейтов может быть **пропущена**. Это должно
быть **явно видно** в дашборде (§50), а не «молча зачтено как пройденное».

**Когда шлюз пропускается:**
- **Ручной топик** (`seed_topics.mode = to_canon`, §1–3): проект стартует сразу с канона — шлюзы
  **Идея** и **Топик** пропущены (`reason: manual_topic_seed`).
- **Пустой шлюз:** на шлюзе нечего апрувить — напр. все персонажи курса пришли из библиотеки и
  берутся как есть (§7), а стилевых фото не добавляли → на `/photos` ноль `review_items`. Шлюз
  пропущен (`reason: empty_gate`).

**Механизм (откуда известно, что пропущен):** воркер ведёт на проекте
`pipeline.projects.gate_status` (jsonb) — карту `шлюз → {state, reason}`, где хранятся ТОЛЬКО
исключения-пропуски. Заполняется двумя путями:
- при создании проекта из `to_canon`-сида воркер сразу пишет
  `{"idea":{"state":"skipped","reason":"manual_topic_seed"}, "topic":{"state":"skipped","reason":"manual_topic_seed"}}`
  (вывод из `seed_topics.mode`);
- дойдя до шлюза и обнаружив ноль `review_items` к апруву — помечает шлюз
  `{"state":"skipped","reason":"empty_gate"}`.

Пройденные/текущие/будущие шлюзы по-прежнему выводятся из `stage`/`media_status`; `gate_status` их
НЕ дублирует, а только перечисляет пропуски. Дашборд читает `gate_status` и рендерит шлюз как
«пропущено» (Приложение D, §50/D3–D4).

**Ключи шлюзов (едины с дашбордом §50/D3):** `idea, topic, canon, photos, prompts, video1, video2,
video3, content, import`.

---

## 5. Что выдаёт P3 (канон курса) и куда уходит каждый слот

P3 НЕ пишет финальные промпты — задаёт КАНОН и слоты, которые заполняют B-генераторы.
Канон = `{ course_facts, narrative, lesson_plan, 3 видео-акта, character roster,
edge_element, hook }`.

Слоты курса (звёздочкой* — достроено после обрыва заметки):

1. Промпт **главной обложки** (центр карточки) ──────────► **B3** `course_illustration`
2. ~~Промпт **мини-картинки** (низ карточки) → B4 `card_decoration`~~ — **УБРАН** (ревью #10/#11: карточка Lore разрежена, декор не рендерится; поле `card_decoration_url` удалено из схемы — §12.3а, §10).
3. **3 видео** (видео-уроки = 3 акта) ──────────────────► **B2** (каждое → 6 kling-промптов)
4. *Обложки уроков* ────────────────────────────────────► **B3** `lesson_cover`
5. *Текст + задания обычных уроков* ────────────────────► **B1** (+ image-брифы → B3)
6. *Картинки в уроках (reading_media, image_recognition)* ► **B3**
7. *Ростер персонажей (имена, число)* ──────────────────► **в каноне** (одобряется при апруве канона)

> Канон полностью редактируем на апруве (§6/этап 5): число уроков, число персонажей, убрать
> конкретного — любым замечанием, сценарий переписывается. БД принимает переменное число и
> уроков, и персонажей (массивы `lessons[]`/`characters[]` любой длины).
> ⚠️ Пункты 4–6 достроены по уже зафиксированному — заметка оборвалась на «3 промпта для…».

---

## 6. Иерархия промптов (верх → низ)

```
ВЕРХ — ВОРОНКА И КАНОН
├── P1  Генератор идей            — 2–3-словные зёрна, без повторов
├── P2  Идея → Топик             — развернуть апрувнутую идею в топик
└── P3  Генератор сценария-курса — КАНОН для всех B (факты, нарратив, план уроков,
                                    3 видео-акта, ростер персонажей, edge/хук)

НИЗ — ГЕНЕРАТОРЫ АРТЕФАКТОВ (бывший монолит P3; каждый отдельный, все едят канон)
├── B1  Текст + задания  — reading_text/reading_media + quiz-типы               [ГОТОВ]
├── B2  Видео (omni)     — 3 видео × 6 kling_prompt → omni-формат (image_list)   [ОБНОВИТЬ]
├── B3  Большие картинки — illustration / lesson_cover / reading_media / image  [ГОТОВ]
├── ~~B4  Угловой декор — card_decoration~~                                       [УБРАН — ревью #10/#11, поле удалено из схемы]
├── B5  Фото персонажей  — refs: character_lock + 1 фото на персонажа (ростер уже в каноне)  [ГОТОВ]
└── B6  Стилевые фото    — набор образцов стиля без персонажей (апрув)           [TODO]

P4  Доработка — точечная перегенерация ОДНОГО элемента по Refine-комментарию.
```

**Канон-контракт на вход каждому B:** `{ course_facts, narrative, lesson_plan, characters,
edge_element, hook }`. «Раздельно» ≠ «вразнобой» — все читают один канон.

---

## 7. Порядок продакшена и апрув визуального фундамента

Ростер персонажей уже одобрен в каноне (§6). Дальше — фото. B2 (omni-видео) и B3 ссылаются
на залоченные референс-фото → их готовим и апрувим ПЕРЕД видео/картинками.

```
0. (опц.) ОПЕРАТОР прикладывает референс-фото стиля ── шлюз /photos просит до B6, см. §15
1. B5 refs → фото персонажей (1 на каждого из ростера канона) ─┐
2. B6      → стилевые фото (без персонажей; если приложен референс — Style Reference 166) ─┤→ ⛔ АПРУВ ФОТО ⟳
   ─────────────────────────────────────────── только потом:    ┘   (персонажи + стиль вместе)
3. B2 omni-видео  (в image_list: фото персонажей + стиль-фото; бюджет ≤ 7, см. §9)
4. B3 визуал      (нужны фото-референсы персонажей; Character Reference 397, §9.5)
5. B4 декор       (персонажи не нужны — параллельно; transparency PNG, §9.5)
```

- **Референс-фото стиля (опционально, шаг 0):** если оператор нашёл картинку, которая идеально
  задаёт желаемый стиль — он прикладывает её на шлюзе `/photos` ДО запуска B6. Воркер грузит
  файл в Leonardo (`POST /init-image` → `initImageId`) и B6 берёт его как Style Reference
  (controlnet 166, §9.5). Не приложено — B6 генерит стиль чисто по тексту. Сам референс — это
  ВХОД, он не апрувится как артефакт; апрувятся уже сгенерённые стиль-фото.

- Отдельного апрува *списка* персонажей нет — список решается в каноне. Апрувится только
  визуальный результат (фото). Один объединённый шлюз: фото персонажей + стилевые фото.
- Зачем шлюз ДО видео: чинить кривое фото дёшево, перегенерировать все клипы — дорого.
- Стилевых фото апрувим столько, сколько влезает в omni: потолок вызова = 7 картинок всего,
  персонажи в приоритете, стилю — остаток (чуть меньше).
- Библиотечные совпадения персонажей по aliases берём как есть (и фото, **и голос**, §9.6) —
  повторно не апрувим.
- Стиль фото персонажа = стиль видео (реально-мультяшный) — задаёт вид во всём медиа.

---

## 8. Анатомия курса

- **Курс** = 6–10 уроков. **Видео-урок** — только видео, позиции **1, 4, 7** (3 акта).
  **Обычный урок** — 6–8 заданий: сначала ВЕСЬ текст, потом ВСЕ quiz.
- Видео двойного назначения: урок в приложении + маркетинг в соцсетях.
- Пример (9): `1-видео·2·3·4-видео·5·6·7-видео·8·9` → 3 видео + 6 обычных.
- Типы заданий §6.5; `requires_check`: чтение — Continue, quiz — проверка.

---

## 8.5. Апрув промптов (перед производством медиа)

После того как B-генераторы выдали все промпты, но ДО дорогой генерации медиа — отдельный
апрув промптов. Каждый промпт показывается с:

- **подробным переводом на русский**,
- **кратким пересказом на русском** (чтобы не вчитываться — быстрый взгляд),
- возможностью развернуть удобный тебе формат и оценить, что сгенерилось.

Refine-комментарий → перегенерённый промпт → снова апрув (цикл §A). Смысл шлюза: кривой
промпт правим дёшево текстом, а не пережигаем кредиты Kling/Leonardo/ElevenLabs.

---

## 9. Производство медиа через Kling Omni API

Видео генерится **omni-моделью** Kling: `POST /v1/videos/omni-video`, модель
`kling-v3-omni` (или `kling-video-o1`). Omni принимает в `image_list` РАЗНЫЕ референс-фото —
и персонажей, и **стиля** — адресуемые в промпте через `<<<image_N>>>`.

**Параметры вызова (наши дефолты):** `duration: "15"`, `mode: "pro"` (1080p),
`aspect_ratio: "9:16"`, `sound: "off"` (озвучку кладём через ElevenLabs).
`multi_prompt` (до 6 раскадровок, суммарно = duration) → сюда ложатся наши нарезки 3–4с.

**Как задаётся стиль (главное новое):** в `image_list` кладём фото-образцы стиля и в
промпте пишем «The video style matches that of `<<<image_k>>>`». Это и есть передача
дизайна примером-картинкой (а не текстом «образца-ролика» — такого входа у Kling нет).

**Бюджет референс-фото в ОДНОМ вызове omni:**
- Жёсткий максимум `image_list` = **7** фото (путь без reference-видео, только image-референсы).
- Если использовать «видео-элементы» персонажей из библиотеки (element_list) — лимит падает
  до 4. Поэтому идём через `image_list` ради бюджета 7.
- **Раскладка бюджета:** фото персонажей — приоритет (по 1 на каждого персонажа в этом видео);
  стилевые фото забирают остаток. То есть `#персонажей + #стиль (+1 если якорь-first_frame) ≤ 7`.
  Стилю выделяем чуть меньше: при 3 персонажах → до 4 стиль-фото; при 4 → до 3; и т.д.
- ⚠️ `kling-video-o1` не поддерживает first/end frame, если в массиве >2 картинок. Значит
  при петле через якорь-кадр + несколько референсов берём `kling-v3-omni` (до 3 элементов с
  first_frame) ИЛИ держим петлю промптом/стилем, а не пиннингом first_frame. — **решение ниже.**

После генерации: ffmpeg склеивает 2×15с в три 30-сек ролика → Cloudflare Stream;
ElevenLabs озвучивает по спикерам → ручная синхронизация рта; Leonardo отдаёт картинки в Storage.

---

## 9.5. Картинки через Leonardo Phoenix API

Все растровые картинки (B3/B4/B5/B6) генерит **Leonardo Phoenix 1.0**
(`modelId = de7d3faf-762f-48e0-b3b7-9d0ac3a3fcf3`). Базовый вызов: `prompt` + `modelId` +
`contrast` (3 / 3.5 / 4). Для quality-режима — `alchemy: true`. Phoenix — единственная
собственная модель Leonardo с полным набором controlnet'ов, поэтому фиксируем её для всего
растра ради единого вида.

**Передача референс-фото — через `controlnets[]`** (массив опционален; нет массива =
чистый text-to-image). Каждый элемент: `{ preprocessorId, initImageId, initImageType,
strengthType }`. `initImageType`: `UPLOADED` (внешнее фото оператора или наше из Storage,
предварительно загруженное через `POST /init-image` → получаем `initImageId`) либо
`GENERATED` (картинка, сделанная внутри Leonardo). `strengthType`: `Low | Mid | High`.

Нужные нам препроцессоры Phoenix:
- **397 — Character Reference** (лицо/образ персонажа). Используется B3.
- **166 — Style Reference** (стиль картинки). Опционально используется B6.
- (есть ещё 364 Content Reference — нам сейчас не нужен.)

**Прозрачный PNG:** параметр `transparency: "foreground_only"` (дефолт `disabled`) — даёт
альфа-канал. Используется B4 (угловой декор).

**Раскладка по генераторам (что реально уходит в вызов):**

| Ген | Что генерит            | Режим вызова Leonardo                                            |
|-----|------------------------|------------------------------------------------------------------|
| B5  | фото персонажа         | **text-to-image** (только `prompt`); без controlnets             |
| B3  | обложки/картинки уроков| **text + фото персонажа**: controlnet 397 (`initImageType:UPLOADED`, фото из B5) |
| B4  | угловой PNG-декор      | **text-to-image** + `transparency: foreground_only`              |
| B6  | стилевые фото          | **text**, опц. + референс стиля оператора: controlnet 166 (`UPLOADED`) |

> ⚠️ Style Reference (166) — это про генерацию КАРТИНОК в Leonardo, не путать с omni
> `image_list` (§9): omni — это передача стиля в видео Kling. B6 сперва генерит стиль-фото
> в Leonardo (опц. опираясь на референс оператора), и уже эти фото потом летят в omni `image_list`.

**Апрув медиа делится на два:**
- **Видео — поштучно (этап 11):** как только готов ролик 1 — он сразу летит тебе на проверку;
  готов 2 — проверка; готов 3 — проверка. Не ждём всю тройку. (`review_items.kind=video_clip`,
  по одному на видео.) **Refine правит НЕ видео, а его `source_prompt`** (refine_rewriter, §A) →
  воркер заново зовёт omni с новым промптом → перегенерация только этого ролика.
- **Всё остальное — батчем (этап 12):** все картинки курса (обложка, мини-картинка, обложки
  уроков, картинки в уроках) + все тексты и задания апрувятся вместе, отдельно от видео.

---

## 9.6. Озвучка через ElevenLabs (обычный TTS, НЕ Agents)

Озвучка — обычный **Text-to-Speech** (`POST /v1/text-to-speech/{voice_id}`), **НЕ** Conversational
Agents (та платформа — живой диалог в реальном времени, для пре-рендера под видео не годится).
omni отдаёт видео `sound:off` (§9) — звук кладём поверх.

**Модель:** дефолт `eleven_v3` (эмоциональная, multi-speaker, audio-теги — под драму §10) для
видео-диалогов; `eleven_multilingual_v2` — стабильный фолбэк/закадровый нарратив.
⚠️ v3 — alpha: проверить поддержку `convert-with-timestamps`; где нужен точный тайминг, а v3 его
не даёт — берём `eleven_multilingual_v2`.

**Голос персонажа = залоченный атрибут (как фото).** Хранится в `pipeline.characters.voice_id`
(+ `voice_settings`). Источник — курируемый пул `pipeline.voice_pool` (готовые голоса библиотеки
с тегами пол/возраст/типаж). ElevenLabs голос сам не подбирает — `voice_id` присваиваем мы.

**Жизненный цикл голоса = жизненный цикл фото — апрувятся ВМЕСТЕ на `/photos`, до видео:**
- **Новый персонаж:** B5 генерит фото + воркер авто-присваивает голос → и фото, и голос апрувятся
  на одном шлюзе `/photos` → пишутся в запись персонажа.
- **Персонаж из библиотеки (совпал по aliases, §7):** фото И голос уже в записи → берём как есть,
  **повторно не апрувим** ни фото, ни голос. Фото выбрано заранее ⇒ голос тоже выбран заранее,
  отдельного шага выбора в этом курсе нет.
- **Запись с фото, но `voice_id = NULL`** (старый/импортированный персонаж): воркер авто-заполняет
  голос при первом использовании → разовый апрув на `/photos` → дальше залочен.
- Следствие: один и тот же персонаж звучит **одинаково во всех курсах** (как и выглядит) — фича
  общей библиотеки, не баг.

**Выбор голоса — РЕШЕНИЕ: авто-пул + ручной выбор из библиотеки** (без промпта/Voice Design):
1. Воркер при заполнении ростера **авто-присваивает** персонажу `voice_id` из `voice_pool` по его
   `gender/age/role`. В 80% случаев оператор ничего не делает — слушает и апрувит.
2. На `/photos` у карточки персонажа — блок «🎙 Голос»: текущий голос + ▶ (плеер на `preview_url`)
   + кнопка «Сменить».
3. «Сменить» разворачивает 5–8 кандидатов (воркер взял через `GET /v2/voices` по параметрам
   персонажа): строка = ▶ `preview_url` + лейблы (пол/возраст/акцент) + «Выбрать». Опц.
   фильтры-выпадашки (пол/возраст/акцент) перезапрашивают список. **Текстового поля/промпта НЕТ —**
   только прослушка и клик.
4. Выбор → `voice_id` в `pipeline.characters`. **Refine** на голосе («дай других / голос постарше»)
   → воркер перезапрашивает `GET /v2/voices` с новыми фильтрами. `review_items.kind=character_voice`.

> Voice Design (генерацию уникального голоса по текстовому описанию) сознательно НЕ берём в UI.
> При необходимости добавляется отдельной вкладкой с промптом, предзаполненным из `character_lock`
> (`POST /v1/text-to-voice/design` → 3 превью → `POST /v1/text-to-voice` фиксирует `voice_id`).

**Генерация и наложение на видео — ПО РЕПЛИКАМ, не одним файлом:**
- B2 выдаёт диалоги по спикерам (§11). Каждая реплика → отдельный вызов `convert-with-timestamps`
  с `voice_id` персонажа → аудиоклип + посимвольный alignment (`character_start/end_times_seconds`).
- `ffmpeg.py` раскладывает клипы по таймлайну немого 30-сек ролика (`adelay` под offset + `amix`);
  тайминги идут и в синхронизацию рта (§9), и в субтитры.
- Формат: `mp3_44100_128` дефолт; для чистого микса — `pcm_44100`/`wav_44100` + финальная конвертация.

**Refine озвучки (§16):** `kind=voiceover`, `content`=ссылка на аудио, `source_prompt`=текст реплики
(+ voice_settings). Правится текст/настройка → перегенерация клипа тем же путём.

**Исторические имена:** TTS принимает `pronunciation_dictionary_locators` — единый по проекту словарь
произношения редких/исторических имён, чтобы звучали одинаково во всех роликах.

---

## 10. Разделение труда: драма против точности

- **Видео (B2)** — драма ×10, точность отключена, comment-bait, soap-opera крючки (+ маркетинг).
- **Текст (B1) и картинки (B3)** — якорь точности: факты, даты, костюм, эпоха верны.

---

## 11. Параметры B-генераторов (статус и файлы)

- **B1** `b1_text_and_tasks_generator.md` [ГОТОВ] — англ. (США); точный, но живой; 2–3 факта;
  2–3 сегмента; 6–8 заданий, текст→quiz; image-брифы для B3.
- **B2** `video_prompt_generator.md` [ОБНОВИТЬ под omni] — 3 видео × 6 раскадровок; вывод в
  omni-формате: `image_list` (фото персонажей + стилевые фото, ≤7), `<<<image_N>>>` в промпте,
  стиль через «video style matches <<<image_k>>>», `duration:15 / mode:pro / 9:16 / sound:off`,
  `multi_prompt` под нарезки; диалоги под ElevenLabs; comment-bait + edge-крючок.
- **B3** `b3_visual_prompt_generator.md` [ГОТОВ] — Leonardo Phoenix; 2 регистра; точная эпоха;
  персонажи **по фото** через Character Reference (см. §9.5): на вход — текстовый промпт +
  ранее сгенерённое фото персонажа(ей). Сцена с реальным человеком обязана ссылаться на его
  фото (controlnet 397), не описывая внешность словами.
- ~~**B4** `b4_card_decoration_generator.md`~~ — **УБРАН** (ревью #10/#11): карточка Lore разрежена
  (логотип + пилюля + заголовок), декор не рендерится; поле `card_decoration_url` удалено из схемы
  (§10, §12.3а). Генератор `b4_card_decoration_generator.md` ретирован (не запускается); остаточные
  упоминания `card_decoration` в перечислениях review-item ниже безвредны — артефакт больше не создаётся.
- **B5** `b5_character_generator.md` [ПРАВКА] — теперь только refs: ростер персонажей идёт из
  канона P3 (режим list не нужен), на каждого — character_lock + 1 представительное фото.
  **Генерация только text-to-image** (по дескриптору, без входных фото, §9.5); готовое фото
  затем переиспользуется в B3 (Character Reference) и в omni `image_list`.
- **B6** `b6_style_photo_generator.md` [TODO] — генератор стилевых фото: набор образцов целевого
  дизайна без персонажей, для передачи стиля в omni `image_list`. Text-to-image; **опционально**
  принимает приложенное оператором референс-фото стиля → Leonardo Style Reference (controlnet 166,
  §9.5). Источник референса — шлюз перед генерацией стиля (§7, §15 `/photos`).

> Все промпты сохраняются в БД (см. §A) — для последующей доработки пайплайна.
> Осталось: верхние P1 / P2 / P3 под эту воронку, и универсальный переписыватель уже готов (refine_rewriter.md).

---

# ЧАСТЬ II — Инфраструктура и реализация

> Добавлено к архитектуре промптов выше. Адаптировано под наши решения: omni-видео,
> B-split, апрувер внутри админки, связь Mac mini ↔ админка **только через Supabase**.
> Tailscale / Jinja-HTMX / туннели НЕ используются.

## 12. Как живёт конвейер (архитектура и связь)

- **Конвейер** — Python-воркер на **Mac mini** (APScheduler + asyncio), держится живым через
  launchd, без сна (pmset). Генерирует контент и медиа, пишет состояние в Supabase.
- **Апрувер** — НЕ отдельное приложение и НЕ на Mac mini. Это **раздел админки** (Приложение B):
  React + shadcn/ui + Tailwind, та же палитра (§5 спеки), адаптив под телефон. Хостинг как у
  админки (Vercel/Netlify), вход по ссылке с авторизацией.
- **Связь — только через Supabase.** Mac mini-во드keр читает/пишет схему `pipeline.*`
  сервисным ключом (локально). Админка читает/пишет `pipeline.*` через **Edge Function**
  (сервисный ключ в браузер не попадает) либо admin-RLS. Прямого соединения админка↔Mac mini
  и туннеля НЕТ — они встречаются в БД.
- **Поток управления:** воркер генерит → пишет в `pipeline.*` → админка показывает на апрув →
  я апрувлю/правлю (запись обратно в `pipeline.*`) → воркер видит при поллинге → двигает стадию
  дальше / запускает доработку. email — только уведомления-ссылки.

```
[Mac mini: Python-воркер]  --service key-->  [ Supabase: схема pipeline.* ]  <--Edge Fn--  [Админка (React/shadcn): апрувер]
   APScheduler + поллер                          (единственная точка встречи)                 /queue /ideas /topic /canon /photos /prompts /videos /content
   Claude/omni/Leonardo/ElevenLabs                                                            email-уведомления со ссылкой
```

## 13. SQL-схема `pipeline` и общение с БД

Отдельная схема `pipeline` в той же Supabase (RLS: сервисная роль для воркера; админка — через
Edge Function / admin-RLS). В `public` импортируется только готовый курс (идемпотентно по `slug`).

```sql
create schema if not exists pipeline;

-- Проект курса: движется по стадиям нашей воронки
create table pipeline.projects (
  id            uuid primary key default gen_random_uuid(),
  stage         text not null default 'idea_pool',
    -- idea_pool|idea_approved|topic_draft|topic_approved|canon_draft|canon_approved
    -- |photos_pending|photos_approved|prompts_pending|prompts_approved
    -- |in_production|published|rejected
  media_status  text,
    -- video1_pending|video1_approved|video2_pending|video2_approved
    -- |video3_pending|video3_approved|content_pending|content_approved|ready_to_mux|done
  gate_status   jsonb,           -- ТОЛЬКО исключения-пропуски: карта шлюз → {state:'skipped', reason}.
                                 --   Пройдено/текущее/будущее выводится из stage/media_status; здесь —
                                 --   лишь skipped-гейты + причина (§4.1). Дашборд (§50/D3–D4) рисует «пропущено».
  slug          text,
  working_title text,
  category      text,
  user_note     text,            -- дописка при апруве топика = USER_BRIEF (доходит до B2)
  idea_json     jsonb,           -- стадия 1 (P1)
  topic_json    jsonb,           -- идея→топик (P2)
  canon_json    jsonb,           -- канон (P3): факты, нарратив, план уроков, 3 видео-акта,
                                 --             ростер персонажей, edge_element, hook
  course_json   jsonb,           -- собранный курс (B1–B4 заполнили слоты) → импорт в public
  media_json    jsonb,           -- ссылки на видео/аудио/картинки + статусы
  imported_course_id uuid,       -- public.courses.id после импорта
  created_at    timestamptz default now(),
  updated_at    timestamptz default now()
);
-- Отличия от старой спеки: нет scenarios_json/chosen_scenario (3 варианта не делаем);
-- добавлен topic_json (шаг идея→топик); scenario→canon переименован.

-- Глобальная библиотека персонажей (переиспользуется между курсами)
create table pipeline.characters (
  id             uuid primary key default gen_random_uuid(),
  canonical_name text not null unique,
  aliases        text[] not null,
  is_historical  bool default true,
  character_lock text,                 -- текстовый дескриптор (бэкап)
  photo_urls     text[] not null,      -- фото-референс(ы); для omni берём 1 в image_list
  leonardo_image_ids text[],           -- initImageId фото в Leonardo (для B3 Character Reference 397, §9.5)
  voice_id       text,                  -- ElevenLabs voice_id (озвучка, §9.6); лочится как фото
  voice_settings jsonb,                 -- {stability, similarity_boost, style, speed} для TTS
  created_at     timestamptz default now()
);
-- kling_subject_id убран: omni берёт фото по URL в image_list, Subject Library не используется.
-- leonardo_image_ids: фото из B5 грузим в Leonardo (POST /init-image) → id переиспользуем в B3.
-- voice_id: голос — залоченный атрибут персонажа (как фото); источник — пул pipeline.voice_pool, §9.6.

create table pipeline.project_characters (
  project_id   uuid references pipeline.projects(id),
  character_id uuid references pipeline.characters(id),
  primary key (project_id, character_id)
);

-- Курируемый пул голосов ElevenLabs (готовые голоса библиотеки), §9.6.
-- Воркер авто-присваивает персонажу голос отсюда по gender/age/tags; ручной override на /photos.
create table pipeline.voice_pool (
  id          uuid primary key default gen_random_uuid(),
  voice_id    text not null unique,    -- ElevenLabs voice_id
  name        text,
  gender      text,                    -- male|female|neutral
  age         text,                    -- young|middle|old
  tags        text[],                  -- типаж: narrator|villain|child|… (для авто-назначения)
  preview_url text,                    -- сэмпл для прослушки в апрувере (<audio src=…>)
  created_at  timestamptz default now()
);

-- Стилевые фото проекта (B6) — образцы дизайна без персонажей, для omni image_list
create table pipeline.style_photos (
  id         uuid primary key default gen_random_uuid(),
  project_id uuid references pipeline.projects(id),
  url        text not null,
  facet      text,                     -- lighting|palette|texture|composition
  approved   bool default false,
  created_at timestamptz default now()
);
-- ВХОДНОЙ референс стиля, приложенный оператором на /photos ДО B6 (§7 шаг 0, §15).
-- Это не артефакт на апрув, а вход: воркер грузит в Leonardo и B6 берёт как Style Reference (166).
create table pipeline.style_refs (
  id                uuid primary key default gen_random_uuid(),
  project_id        uuid references pipeline.projects(id),
  url               text not null,     -- загруженный оператором файл (наш Storage)
  leonardo_image_id text,              -- initImageId после POST /init-image (для controlnet 166)
  created_at        timestamptz default now()
);

-- Свои темы вручную (приоритетные семена)
create table pipeline.seed_topics (
  id         uuid primary key default gen_random_uuid(),
  title      text not null,
  premise    text,
  user_note  text,                     -- кусок сценария / дописка к промпту
  mode       text default 'to_canon',  -- to_canon (готовый топик) | to_idea_batch
  priority   bool default true,
  status     text default 'pending',   -- pending | consumed
  created_at timestamptz default now()
);

-- Поэлементный апрув: каждый проверяемый элемент — строка
create table pipeline.review_items (
  id             uuid primary key default gen_random_uuid(),
  project_id     uuid references pipeline.projects(id),
  kind           text not null,
    -- idea|topic|canon|character_photo|character_voice|style_photo
    -- |prompt (любой B-промпт, с RU-переводом+пересказом)
    -- |video_clip (по одному на видео)
    -- |reading_text|reading_media_text|matching|fill_blank|image_recognition
    -- |multiple_choice|true_false|timeline
    -- |course_meta|cover_image|illustration|card_decoration|reading_media_image|image_recognition_image|voiceover
    -- (НЕТ retelling, НЕТ auto_check, НЕТ is_feed — сознательно, см. §18)
  locator        jsonb not null,       -- путь внутрь canon/course_json
  content        jsonb,                -- текущее значение (для медиа — ссылка на файл, что показать в плеере)
  source_prompt  jsonb,                -- для МЕДИА (video_clip/*_image/voiceover): промпт, которым
                                       --   это сгенерено. Refine на медиа-шлюзе правит ИМЕННО ЕГО
                                       --   (refine_rewriter) → воркер заново зовёт генератор → новое
                                       --   медиа. Само медиа не редактируется, см. §A. NULL для не-медиа.
  content_ru     jsonb,                -- для kind=prompt: RU-перевод + краткий RU-пересказ (Этап 10)
  status         text default 'pending', -- pending|approved|needs_revision|regenerating
  comment        text,                 -- моё замечание (Refine)
  revision_count int default 0,
  version_history jsonb,               -- все версии артефакта + замечания (прозрачность варианта 2)
  created_at     timestamptz default now(),
  updated_at     timestamptz default now()
);

create table pipeline.events (
  id uuid primary key default gen_random_uuid(),
  project_id uuid references pipeline.projects(id),
  type text not null, payload jsonb, created_at timestamptz default now()
);
```

**Общение с БД:** воркер — `supabase-py` сервисным ключом; админка — через Edge Function
`pipeline-approval` (валидация + запись статуса/замечания) и чтение `pipeline.*`. Импорт в
`public` — `importer.py`, идемпотентно по `slug`.

## 14. Файловая структура кода

```
pipeline/                         # Mac mini, Python-воркер
├── main.py                       # APScheduler + поллер Supabase (asyncio)
├── scheduler.py                  # ежедневная генерация идей, heartbeat
├── poller.py                     # видит approved/needs_revision в pipeline.* → действует
├── stages/
│   ├── s1_ideas.py               # P1 — идеи
│   ├── s2_topic.py               # P2 — идея→топик
│   ├── s3_canon.py               # P3 — сценарий-канон
│   ├── b1_text.py … b6_style.py  # B1–B6 — заполнение слотов
│   └── refine.py                 # универсальный переписыватель (refine_rewriter)
├── media/
│   ├── omni.py                   # Kling omni через fal.ai (image_list, multi_prompt)
│   ├── leonardo.py               # Phoenix 1.0: картинки/декор/фото персонажей/стиль-фото;
│   │                             #   POST /init-image (upload) + controlnets 397/166 + transparency (§9.5)
│   ├── elevenlabs.py             # озвучка по спикерам
│   └── ffmpeg.py                 # склейка 2×15с → 30с
├── importer.py                   # course_json → public (идемпотентно по slug)
├── db.py                         # supabase-py (service key), CRUD pipeline.*
├── notify.py                     # email-уведомления (ссылка на раздел апрувера в админке)
└── config.py                     # .env

admin/                            # Vercel/Netlify, React + shadcn/ui (Приложение B)
├── src/pages/queue/QueuePage.tsx # /queue — витрина очереди
└── src/pages/approval/           # детальные экраны апрува (см. §15)

supabase/functions/
└── pipeline-approval/            # Edge Function: апрув/реджект + серверная валидация
```

Зависимости воркера: `anthropic`, `apscheduler>=3.10`, `supabase>=2`, `httpx`, `elevenlabs`,
`python-dotenv`. (Без fastapi/uvicorn/jinja — UI в админке.)

## 15. Веб-страницы апрува (в админке) + UX

Все — разделы админки (React + shadcn/ui), адаптив веб+телефон. На каждой — механика §A
(Approve / Reject / Refine), плюс анти-случайный-тап: Approve заблокирован без галочки
«I reviewed»; Reject — без галочки + непустого комментария; необратимое («в продакшен»,
импорт) — двойное подтверждение; валидация на сервере (Edge Function).

| Маршрут        | Шлюз (этап)                         | Что показывает / UX |
|----------------|-------------------------------------|---------------------|
| `/queue`       | витрина всех проектов               | карточки: прогресс-бар, степпер (пройдено/текущее/будущее/**пропущено**, §4.1), бейдж, флаг «ждёт меня» наверх; CTA на нужный экран |
| `/ideas`       | апрув идей (эт. 2)                   | список идей-зёрен, Approve/Reject |
| `/topic`       | апрув топика (эт. 4)                 | топик; Reject / Refine+поле / Approve+опц. дописка (USER_BRIEF) |
| `/canon`       | апрув канона (эт. 5/6)              | весь канон: план уроков, **ростер персонажей с числом** (правлю любым замечанием — число уроков/персонажей/убрать конкретного); персонажи одобряются здесь |
| `/photos`      | апрув фото (эт. 7+8)                | (опц.) **аплоадер «приложить референс-фото стиля»** ДО генерации B6 → Style Reference (166, §9.5); затем фото персонажей + стилевые фото вместе; **у карточки персонажа блок «🎙 Голос»**: авто-голос из пула + ▶ прослушка (`preview_url`) + «Сменить» (список кандидатов из `GET /v2/voices`, фильтры пол/возраст/акцент, без промпта; §9.6); до видео |
| `/prompts`     | апрув промптов (эт. 10)             | каждый промпт + **RU-перевод + краткий RU-пересказ**, разворот удобного формата; Refine |
| `/videos`      | апрув видео (эт. 11)                | видеоплеер на каждый ролик; **поштучно** — как только готов ролик, сразу сюда. Refine правит **промпт** (`source_prompt`), не видео → перегенерация ролика (§A) |
| `/content`     | апрув остального (эт. 12)           | **батч**: все картинки курса + все тексты/задания, поэлементно. Refine на картинке/озвучке правит **промпт** (`source_prompt`) → перегенерация (§A) |

Переход дальше — только когда все элементы текущего шлюза `approved`.

## 16. Надёжность

- **Редактирование медиа НЕДОСТУПНО — правим промпт, не файл (инвариант воркера).**
  Готовое видео/фото/декор/озвучку мы НЕ редактируем (такой операции нет). Не нравится результат —
  **шаг назад: правим промпт, которым это сгенерено**, и гоним заново. Конкретно:
  1. При создании любого медиа-`review_item` (`video_clip` / `*_image` / `card_decoration` /
     `voiceover`) воркер **обязан скопировать в `source_prompt`** тот промпт, которым медиа
     сгенерено (из соответствующего `kind=prompt`, этап 10). Без этого Refine нечего переписывать.
  2. Refine на медиа-шлюзе → `poller.py` зовёт `stages/refine.py` (`refine_rewriter.md`,
     `artifact_type = video_omni_shots | image_prompts | card_decoration | voiceover`,
     `current_version = source_prompt`, `user_remark = моё замечание`) → новый чистый промпт.
  3. Воркер кладёт новый промпт в `source_prompt`, старый уезжает в `version_history`, и заново
     вызывает генератор (`media/omni.py` / `leonardo.py` / `elevenlabs.py`) — **тем же путём, что
     и первичная генерация**, только с новым промптом. Результат → ссылка в `content` → снова на апрув.
  4. `content` (ссылка на медиа) при этом перезаписывается на новый файл; само медиа никогда не
     мутируется. Это и есть «откат на шаг назад к промпту» (§A, P4).
- **Ретраи:** `httpx` с backoff. Кривой JSON от LLM → авто-повтор «return valid JSON only».
- **Деградация:** падение omni/Leonardo/ElevenLabs → проект застывает на текущем `media_status`,
  email-алерт, повтор кнопкой в админке.
- **Kill-switch:** `PIPELINE_ENABLED=false` (env) — стоп всей автогенерации одним переключателем.
- **Идемпотентность:** импорт по `slug`; антидубли идей (`avoid_list` в P1); антидубли персонажей
  (поиск по aliases). Каждый шаг пишет `pipeline.events`.
- **Версионирование:** все версии артефактов + замечания в `review_items.version_history`
  (прозрачность переписывателя, §A).
- **Heartbeat:** email раз в сутки «конвейер жив».
- **Секреты:** только в `.env` на Mac mini; сервисный ключ — только в воркере, никогда в браузере.

## 17. Бюджет и доступ к Kling (fal.ai)

- **Доступ к Kling — через fal.ai (PAYG).** НЕ прямой Kling API: для одиночки прямой API —
  стартовый пакет ~$4200 за ~30k единиц + ~3 мес обязательств. fal.ai — pay-as-you-go без
  предоплаты, тот же ключ уже под omni и (опц.) под картинки (Flux).
- **Математика видео (порядок величин, omni pro ~7 кредитов/сек или ~$0.05/сек — проверить
  актуальный тариф omni на fal.ai):**
  - 1 ролик = 2×15с = 30с генерации → ~210 кредитов / ~$1.5.
  - 1 курс = 3 ролика = 90с → ~630 кредитов / ~$4.5 (без переделок).
  - **Буфер +20–30%** — проваленные генерации списывают кредиты без возврата.
- **Картинки:** Leonardo Phoenix 1.0 (§9.5), ~$0.01–0.02 за фото персонажа; обложки/декор/стиль-фото —
  копейки. `alchemy: true` (quality-режим) тратит больше токенов на генерацию — закладываем для
  обложек/фото персонажей; для углового декора можно без alchemy.
- **Озвучка:** ElevenLabs — центы за минуту; переозвучка точечно.
- **Инфра:** Supabase free / хостинг админки (Vercel/Netlify free) / email — $0. Tailscale не используется.

## 18. Что сознательно НЕ берём

- **`retelling`** (тип задания) — не нужен, в типах §6.5 спеки его нет.
- **`auto_check`** (поле) — убрано: все задания с проверкой требуют явного `Done` (§6.5а).
- **3 варианта сценария** — у нас итеративный единый канон, лучше.
- **`is_feed` / TikTok-лента внутри приложения** — вырезано целиком.
- **n8n / Make** — вся оркестрация на Python.
- **Midjourney** — картинки на Leonardo/Flux (C1a спеки); Midjourney не добавляем.
- **ElevenLabs Agents** — это живой разговорный агент (real-time); озвучка идёт обычным TTS (§9.6).
- **Voice Design в UI апрувера** — выбор голоса = только готовые из пула/библиотеки (§9.6); генерацию
  голоса по промпту в интерфейс не выносим (бэкап-вариант описан в §9.6, но по умолчанию выключен).
- **Tailscale / Jinja-HTMX / туннель** — апрувер в админке, связь через Supabase.
